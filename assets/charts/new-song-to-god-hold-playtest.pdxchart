# Hold overlay for the local NewSongToGod legacy tap chart.
# The runtime preserves every imported .stp tap and adds only these original holds.
schemaVersion=1
id=new-song-to-god-hold-playtest

[tempo]
0=142

[notes]
# Each range was selected from an empty span in the matching legacy lane.
hold=SW,13,17,8
hold=C,30,33,6
hold=SE,50,55,10
hold=NE,69,75,12
hold=SW,93,96,8
hold=NE,93,96,12
